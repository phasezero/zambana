# File for preconfigure changes in elasticsearch.yml
# may additional configuration might needs to be done @ the hostsystem
# read: https://docs.zammad.org/en/latest/install/elasticsearch.html
# and https://www.elastic.co/guide/en/elasticsearch/reference/master/docker.html#_set_vm_max_map_count_to_at_least_262144s
# and https://www.elastic.co/guide/en/elasticsearch/reference/current/docker.html


# Allows the engine to generate larger (more complex) search queries.
# Elasticsearch will raise an error or deprecation notice if this value is too low,
# but setting it too high can overload system resources (Default: 1024).
#
# Available in version 6.6+ only.
indices.query.bool.max_clause_count: 2000